@{
}

@model IEnumerable<string>

<h1>@ViewBag.SelectedCounty</h1>

<div class="d-grid gap-2">

    <a asp-controller="Home" asp-action="Index" asp-route-county="" class="btn btn-outline-secondary">Home</a>

    @foreach (string c in Model)
    {
        <a asp-controller="Home" asp-action="Index" asp-route-county="@c" class="btn @(ViewBag.SelectedCounty == c ? "btn-primary" : "btn-outline-secondary")">@c</a>

    }
</div>

